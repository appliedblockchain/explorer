{
  "name": "block-explorer-client",
  "version": "0.1.0",
  "private": true,
  "browserslist": {
    "development": [
      "last 2 chrome versions"
    ],
    "production": [
      "> 1%",
      "not IE <= 11"
    ]
  },
  "proxy": "http://localhost:3001/",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "serve": "serve --single --port 3000 build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "node-sass": "^4.9.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "^2.0.0-next.66cc7a90",
    "serve": "^6.5.7"
  }
}
